<%= simple_form_for [@note] do |f| %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.input :colour,
            collection: Note.colours.keys,
            wrapper_html: { class: "mb-0" } %>
  <%= f.submit %>
<% end %>
