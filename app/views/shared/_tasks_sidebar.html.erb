      <nav class="nav flex-column">
  <!-- Get Projects -->
  <% @projects.each do |project| %>
    <% if project.user_ids.include?(current_user.id) || current_user.id == project.created_by %>
      <div class="card border border-secondary bg-white text-center rounded-lg mb-3">
        <div class="card-body p-0 py-1">
          <%= link_to "#{project.name}", project_path(project) %>
        </div>
      </div>
    <% end %>
  <% end %>
  <% if current_user.position != "employee" %>
    <div class="card border border-secondary bg-white text-center rounded-lg">
      <div class="card-body p-0 py-1">
        <i class="bi bi-plus" style="font-size: 1.75rem;"></i>
        <a class="card-title mb-0 mt-n2"><%= link_to "Create New Project", new_project_path(), class: "nav-link stretched-link pt-0" %></h5>
      </div>
    </div>
  <% end %>
</nav>
